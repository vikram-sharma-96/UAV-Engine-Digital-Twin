{
  "name": "uav-engine-digital-twin",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --test tests/*.test.ts",
    "server": "node scripts/server.ts",
    "desktop": "node scripts/desktop.ts",
    "benchmark": "node scripts/benchmark.ts",
    "dataset": "node scripts/generate_dataset.ts",
    "backend": "uvicorn backend.main:app --reload --host 127.0.0.1 --port 8000"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.2",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "esbuild": true
  }
}
